CREATE TABLE events (
    title TEXT NOT NULL,
    day TEXT NOT NULL,
    month TEXT NOT NULL,
    year TEXT NOT NULL,
    hour TEXT NOT NULL,
    minutes TEXT NOT NULL,
    repeat TEXT NOT NULL,
    username TEXT NOT NULL
);
